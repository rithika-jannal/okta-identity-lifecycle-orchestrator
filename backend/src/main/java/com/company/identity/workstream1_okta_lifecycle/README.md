# Workstream 1 — Okta & Lifecycle

Members: Member 1 + Member 2

Member 1 owns `okta/`.
Member 2 owns `lifecycle/` and `policy/`.

Member 1 provides Okta operations.
Member 2 builds Joiner, Mover, Leaver and policy logic.

Do not let other workstreams duplicate Okta API code.
